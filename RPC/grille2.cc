#include <bits/stdc++.h>
#define MAXN 11
using namespace std;


int main()
{
  char matrix[MAXN][MAXN];
  int n; cin >> n;
  for(int i = 0 ; i < n ; ++i)
    for(int j = 0 ; j < n ; ++j)
      cin >> matrix[i][j];
  return 0;
}
